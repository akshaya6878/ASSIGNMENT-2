#include<stdio.h>
int main()
{
    int numb,temp_var,sum=0;
    printf("enter number");
    scanf("%d",&numb);
    while(numb>=1)
    {
    temp_var=numb%10;
    sum=sum+temp_var;
    numb=numb/10;
    }
    printf("sum of digits is %d",sum);
    return 0;
}
